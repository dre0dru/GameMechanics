using System;

namespace Dre0Dru.Projectiles.Patterns
{
    [Serializable]
    public struct LinePattern
    {
        //TODO add to both line/arc offset output?
        public float CalculateOffset(int index, int count)
        {
            throw new System.NotImplementedException();
        }
    }
}
